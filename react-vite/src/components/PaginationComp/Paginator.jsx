import "./paginator.css";
import Pagination from "react-bootstrap/Pagination";
import "./bootstrap.css";

const Paginator = ({ page, per_page }) => {
  return (
    <>
      <select>
        <option></option>
      </select>
    </>
  );
};

export default Paginator;
